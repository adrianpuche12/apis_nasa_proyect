import styles from "../styles_modules/styles.loading.module.css"

const Loading = () => {
    return (
        <>
            <p className={styles.title}>loading...</p>
        </>
    )
}

export default Loading;